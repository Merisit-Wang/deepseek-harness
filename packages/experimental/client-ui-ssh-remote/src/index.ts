/**
 * Node half of the SSH remote workspace UI plugin. The browser half lives in
 * `./client`; this entry only satisfies the Cordis plugin contract.
 *
 * @module @deepseek-ai/dsh-experimental-client-ui-ssh-remote
 */

/**
 * Empty host-side apply: the panel is browser-only.
 */
export function apply(): void {}
